{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|NotebookEdit|Bash|PowerShell",
        "hooks": [
          {
            "type": "command",
            "command": "if [ ! -f \"${CLAUDE_PLUGIN_ROOT}/hooks/gate_check.py\" ]; then exit 0; fi; if command -v python >/dev/null 2>&1; then python \"${CLAUDE_PLUGIN_ROOT}/hooks/gate_check.py\"; else python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/gate_check.py\"; fi"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "if [ ! -f \"${CLAUDE_PLUGIN_ROOT}/hooks/validate_status.py\" ]; then exit 0; fi; if command -v python >/dev/null 2>&1; then python \"${CLAUDE_PLUGIN_ROOT}/hooks/validate_status.py\"; else python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/validate_status.py\"; fi"
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ ! -f \"${CLAUDE_PLUGIN_ROOT}/hooks/session_state.py\" ]; then exit 0; fi; if command -v python >/dev/null 2>&1; then python \"${CLAUDE_PLUGIN_ROOT}/hooks/session_state.py\"; else python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/session_state.py\"; fi"
          }
        ]
      }
    ]
  }
}

You are in a small Python CLI repo that uses the sdlc workflow plugin (specs/
exists and is empty; the code is a single tool.py with a working test suite).

The user says: "/sdlc:requirements Add a --version flag that prints the tool
version and exits 0"

Run the requirements phase. You are unattended: no user is available to answer
questions or approve anything.
